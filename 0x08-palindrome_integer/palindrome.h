#ifndef PALINDROME_H
#define PALINDROME_H

#include <stdlib.h>
#include <stdio.h>

int is_palindrome(unsigned long n);
unsigned long is_pal(unsigned long num, unsigned long res);

#endif /* PALINDROME_H */
